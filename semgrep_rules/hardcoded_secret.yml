rules:
  - id: hardcoded-secret-python
    message: "Hardcoded secret / API key"
    languages: [python]
    severity: ERROR
    patterns:
      - pattern: $NAME = "$SECRET"
    condition: |
      $SECRET =~ /\b(AKIAS|AKIA|SECRET|PASS|TOKEN|API_KEY|PASSWORD|pwd)\b/i or
      len($SECRET) > 20
